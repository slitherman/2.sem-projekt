@page
@model _2._sem_projekt_boglistesystemet.Pages.Crud.IndexModel

@{
    ViewData["Title"] = "Underviserindex";
}

<h1>UIndex</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               @* @Html.DisplayNameFor(model => model.Underviser[0].FirstName)*@
               Underviser Navn
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Underviser[0].LastName)*@
                Underviser Efternavn
            </th>
            <th>
               @* @Html.DisplayNameFor(model => model.Underviser[0].Initials)*@
               Intitialer
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@*@foreach (var item in Model.Underviser) {*@
        <tr>
            <td>
                @*@Html.DisplayFor(modelItem => item.FirstName)*@
                Egon
            </td>
            <td>
                @*@Html.DisplayFor(modelItem => item.LastName)*@
                Olsen
            </td>
            <td>
                @*@Html.DisplayFor(modelItem => item.Initials)*@
                Eo
            </td>
            <td>
               @* <a asp-page="./Edit" asp-route-id="@item.UnderviserId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.UnderviserId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.UnderviserId">Delete</a>*@
            </td>
        </tr>
@*}*@
    </tbody>
</table>
